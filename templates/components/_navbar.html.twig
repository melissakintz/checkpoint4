<!-- navbar -->
<nav class="font-sans flex flex-col text-center sm:flex-row sm:text-left sm:justify-between py-4 px-6 bg-white shadow sm:items-baseline w-full">
    <div class="mb-2 sm:mb-0">
        <a href="{{ path('app_home') }}" class="text-2xl no-underline text-grey-darkest hover:text-blue-dark">CollectionBoard</a>
    </div>
    <div>
        {% if app.user %}
        <a href="{{ path("board") }}" class="text-lg no-underline text-grey-darkest hover:text-blue-dark ml-2">
            <ion-icon name="clipboard-outline"></ion-icon>
            Board
        </a>
        {% else %}
            <a href="{{ path("app_register") }}" class="text-lg no-underline text-grey-darkest hover:text-blue-dark ml-2">
                Inscription
            </a>
        {% endif %}
    </div>
</nav>
